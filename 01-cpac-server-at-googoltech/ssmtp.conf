#
# Config file for sSMTP sendmail
#
# The person who gets all mail for userids < 1000
# Make this empty to disable rewriting.
root=huang.mb@googoltech.com

# The place where the mail goes. The actual machine name is required no 
# MX records are consulted. Commonly mailhosts are named mail.domain.com
mailhub=smtp.exmail.qq.com:465

# Where will the mail seem to come from?
#rewriteDomain=

# The full hostname
hostname=cpac

# Are users allowed to set their own From: address?
# YES - Allow the user to specify their own From: address
# NO - Use the system generated From: address
#FromLineOverride=YES
#
# gitlab_rails['smtp_enable'] = true
# gitlab_rails['smtp_address'] = "smtp.exmail.qq.com"
# gitlab_rails['smtp_port'] = 465
# gitlab_rails['smtp_user_name'] = "huang.mb@googoltech.com"
# gitlab_rails['smtp_password'] = "KEmw4zfcBj5gVJpg"
# gitlab_rails['smtp_domain'] = "exmail.qq.com"
# gitlab_rails['smtp_authentication'] = "login"
# gitlab_rails['smtp_enable_starttls_auto'] = false
# gitlab_rails['smtp_tls'] = true

AuthUser=huang.mb@googoltech.com
AuthPass=KEmw4zfcBj5gVJpg
UseTLS=YES
UseSTARTTLS=NO
FromLineOverride=YES

